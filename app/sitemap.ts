import type{MetadataRoute}from"next";export default function sitemap():MetadataRoute.Sitemap{return["","/peace-monitor","/privacy","/terms"].map(path=>({url:`https://halftimedad.co${path}`,lastModified:new Date(),changeFrequency:path===""?"weekly":"monthly",priority:path===""?1:.5}))}
